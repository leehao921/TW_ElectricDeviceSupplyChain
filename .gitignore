__pycache__/
*.pyc
*.egg-info/
.venv/
venv/
.DS_Store
*.tmp
*.log
_temp_fix_*.py
enrichment*.json
.claude/settings.local.json

# Scheduler runtime config — copy from docker/.env.example, fill locally
docker/.env

# Claude Code agent scratch worktrees
.claude/worktrees/

# Graphiti bootstrap runtime state (skip-lists, log snapshots)
.graph_*.txt
.graph_bootstrap_*.log
.graph_bootstrap_snapshot_*.log

# --- Analysis report artifacts (regenerate from .md sources) ---
# See docs/git-policy.md §1 "analysis/reports/ rule"
analysis/reports/*.pdf
analysis/reports/*.html

# Ephemeral tick / quote / scan CSV dumps. Re-pull from trading-timescaledb.
# Allow specific CSVs by overriding with `git add -f` when they back an .md claim.
analysis/reports/tick_*.csv
analysis/reports/ofi_*.csv
analysis/reports/snapshot_*.csv

# Editor & IDE
.vscode/
.idea/
*.swp
*~

# Notebook checkpoints
.ipynb_checkpoints/

# Asia panel pipeline outputs — regenerable from scripts/{fetch,validate,compute}_asia_*.py
# Source of truth is trading-timescaledb (see CLAUDE.md "Purpose")
data/asia_market_panel.parquet
data/cov_3y.parquet
data/corr_3y.parquet
data/rolling_corr_twii.parquet
data/twii_dxy_beta.parquet
data/regime_shifts.parquet
data/twii_corr_ranked.parquet
data/raw/

# Note: Vera Rubin VR200 BOM parquets (data/vera_rubin_bom/) ARE tracked —
# they are research outputs from a parallel batch, not regenerable from scripts.

# Obsidian per-user workspace config (vault content stays tracked)
vault/.obsidian/
