__pycache__/
*.pyc
*.egg-info/
.venv/
venv/
.DS_Store
*.tmp
*.log
_temp_fix_*.py
enrichment*.json
.claude/settings.local.json

# Scheduler runtime config — copy from docker/.env.example, fill locally
docker/.env

# Claude Code agent scratch worktrees
.claude/worktrees/

# Graphiti bootstrap runtime state (skip-lists, log snapshots)
.graph_*.txt
.graph_bootstrap_*.log
.graph_bootstrap_snapshot_*.log

# --- Analysis report artifacts (regenerate from .md sources) ---
# See docs/git-policy.md §1 "analysis/reports/ rule"
analysis/reports/*.pdf
analysis/reports/*.html

# Ephemeral tick / quote / scan CSV dumps. Re-pull from trading-timescaledb.
# Allow specific CSVs by overriding with `git add -f` when they back an .md claim.
analysis/reports/tick_*.csv
analysis/reports/ofi_*.csv
analysis/reports/snapshot_*.csv

# Editor & IDE
.vscode/
.idea/
*.swp
*~

# Notebook checkpoints
.ipynb_checkpoints/

# BB squeeze daily alert — live consolidation state file (regenerates each run)
data/bb_consolidation_state.json

# Routine watchdog — live per-job cooldown/alert state (regenerates each run)
data/routine_watchdog_state.json

# Memory cycle monitor auto-generated outputs
data/memory_cycle_state.json
docs/analysis/memory_cycle_*.md

# Memory cycle monitor live inputs (use .example.yaml as reference)
data/memory_cycle_inputs.yaml

# P/B percentile engine — cutoff cache (regenerates on refresh)
data/pb_percentile_cache.json
